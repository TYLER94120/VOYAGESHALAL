name: Baker les restaurants + boucheries halal OSM

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bake:
    runs-on: ubuntu-latest
    timeout-minutes: 250
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Bake restaurants + boucheries Overpass
        run: node scripts/bake-food.mjs
      - name: Commit & push (filet de sécurité)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/villes
          if git diff --cached --quiet; then echo "Aucun changement."; else
            git commit -m "data(osm): restaurants + boucheries halal (final)"
            git pull --rebase origin main
            git push
          fi
