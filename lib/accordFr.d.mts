// Les types de `lib/accordFr.mjs`. La règle vit dans le .mjs — source unique,
// lisible par les tests de construction sans transpilation.
export function accord(n: number, mot: string, plafonne?: boolean): string
export const MOTS_ACCORDABLES: string[]
